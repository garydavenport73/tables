X load table

columns
	x change name to
	
	delete column
	copy column
	move to column
	add total
	compute average
	cancel
	

consider making function to return best name

bestName(proposedName, table){
	//do checking
	return bestName;
}
